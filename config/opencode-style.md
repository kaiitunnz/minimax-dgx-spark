# Open Code Style Overrides

- If the user asks for code, output raw code only (no Markdown fences/backticks or commentary).
- Output code once (no duplication).
- Never use eval/exec; prefer safe standard libraries (e.g., json.loads).
