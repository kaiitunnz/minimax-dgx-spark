# Recipe: MiniMax-M2.7-NVFP4 for dual-DGX-Spark (TP=2).
# NVFP4 is the FP4 quant tuned for NVIDIA Blackwell. Backends are pinned
# explicitly (--quantization modelopt_fp4, --attention-backend flashinfer,
# --moe-backend flashinfer_cutlass) to avoid the sm_120 auto-detect paths
# that historically hit cutlass FP4 / EP / PP issues.
# Currently slower than the AWQ recipe on this cluster; see runbook.md.

recipe_version: "1"
name: MiniMax-M2.7-NVFP4-DGXSparkx2
description: vLLM serving MiniMax-M2.7-NVFP4 across 2x DGX Spark via tensor parallelism

model: lukealonso/MiniMax-M2.7-NVFP4
container: spark-vllm
cluster_only: true
mods: []

env:
  VLLM_NVFP4_GEMM_BACKEND: flashinfer-cutlass
  VLLM_USE_FLASHINFER_MOE_FP4: 1
  VLLM_ALLOW_LONG_MAX_MODEL_LEN: 1

defaults:
  port: 8080
  host: 0.0.0.0
  tensor_parallel: 2
  gpu_memory_utilization: 0.85
  max_model_len: 196608
  max_num_seqs: 4
  max_num_batched_tokens: 8192

command: |
  vllm serve lukealonso/MiniMax-M2.7-NVFP4 \
      --trust-remote-code \
      --port {port} \
      --host {host} \
      --gpu-memory-utilization {gpu_memory_utilization} \
      -tp {tensor_parallel} \
      --max-model-len {max_model_len} \
      --max-num-seqs {max_num_seqs} \
      --max-num-batched-tokens {max_num_batched_tokens} \
      --quantization modelopt_fp4 \
      --kv-cache-dtype fp8 \
      --attention-backend flashinfer \
      --moe-backend flashinfer_cutlass \
      --enable-prefix-caching \
      --distributed-executor-backend ray \
      --load-format fastsafetensors \
      --enable-auto-tool-choice \
      --tool-call-parser minimax_m2 \
      --reasoning-parser minimax_m2
